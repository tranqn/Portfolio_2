/** A single skill entry displayed in the skills grid. */
export interface SkillItem {
  /** Path to the skill's SVG icon. */
  icon: string;
  /** Display name shown below the icon. */
  name: string;
}
