/** A single character used in the hero section's animated title split. */
export interface Letter {
  /** The individual character. */
  char: string;
  /** Whether the character is an uppercase letter (not a symbol or digit). */
  isUpperCase: boolean;
}
